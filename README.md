# TASK-7

# Task 7: Basic Sales Summary using SQLite and Python

## Description
This project connects to a SQLite database and fetches basic sales summaries like total quantity and revenue per product. It then visualizes the results using a bar chart.

## Tools Used
- Python (sqlite3, pandas, matplotlib)
- SQLite

## How to Run
1. Make sure `sales_data.db` exists (run `create_db.py` first).
2. Run `sales_summary.py` to print and visualize data.

## Sample Output(attached in this task file )
